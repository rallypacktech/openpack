{
  "name": "BarcodeCorrection",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional free-form description (up to 1000 characters)"
    },
    "barcode": {
      "type": "string",
      "description": "The UPC/barcode that was scanned"
    },
    "ai_suggested_name": {
      "type": "string",
      "description": "What the AI lookup suggested"
    },
    "ai_suggested_category": {
      "type": "string"
    },
    "corrected_name": {
      "type": "string",
      "description": "What the user corrected it to"
    },
    "corrected_category": {
      "type": "string"
    },
    "corrected_shelf_life_days": {
      "type": "number"
    },
    "submitted_to_api": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "barcode",
    "corrected_name",
    "corrected_category"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}