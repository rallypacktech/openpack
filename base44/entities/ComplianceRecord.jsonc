{
  "name": "ComplianceRecord",
  "type": "object",
  "properties": {
    "record_type": {
      "type": "string",
      "enum": [
        "staff_certification",
        "fire_equipment",
        "home_device"
      ],
      "description": "Whether this tracks a staff certification, a workplace fire/life-safety asset, or a household safety device"
    },
    "title": {
      "type": "string",
      "description": "What is being tracked (e.g. 'CPR / AED Certification', 'Fire extinguisher \u2014 Kitchen')"
    },
    "holder_name": {
      "type": "string",
      "description": "Staff member holding the certification (staff_certification only)"
    },
    "certification_type": {
      "type": "string",
      "enum": [
        "cpr",
        "first_aid",
        "aed",
        "cpr_aed",
        "first_aid_cpr_aed",
        "fire_safety",
        "other"
      ],
      "description": "Certification type (staff_certification only)"
    },
    "equipment_type": {
      "type": "string",
      "enum": [
        "smoke_alarm",
        "carbon_monoxide_alarm",
        "fire_extinguisher",
        "exit_sign",
        "emergency_lighting",
        "sprinkler_system",
        "fire_alarm",
        "aed",
        "other"
      ],
      "description": "Device or equipment type (fire_equipment and home_device records)"
    },
    "location": {
      "type": "string",
      "description": "Building, floor, or room where the equipment lives"
    },
    "issued_date": {
      "type": "string",
      "format": "date",
      "description": "Date the certification was earned, or the equipment was last checked / serviced"
    },
    "expiration_date": {
      "type": "string",
      "format": "date",
      "description": "Date the certification lapses or the next inspection is due"
    },
    "interval_months": {
      "type": "number",
      "description": "How often this needs checking, in months. Drives the one-tap 'log check' action that advances the next due date automatically."
    },
    "subscription_id": {
      "type": "string",
      "description": "Reference to the BusinessSubscription this record belongs to. Empty for household devices."
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "record_type",
    "title",
    "expiration_date"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}